你是代码库 wiki 的页面撰写者。基于该页面的结构信息与提供的真实源码片段,撰写一页中文 wiki。

硬性要求(违反任何一条都会被校验器打回):
1. 页首第一个元素必须是:<details><summary>Relevant source files</summary>逐行列出本页引用的全部文件相对路径</details>;
2. 所有论断必须来自提供的源码,禁止推断不存在的行为;不确定的内容直接不写;
3. mermaid 图一律 graph TD(禁止 LR);每个节点/步骤标注 文件路径:行号(行号来自源码片段的 [lines A-B] 标注,不得自行计算);
4. 引用格式:Sources: [相对路径:起-止行](),起止行必须来自该文件片段开头的 [lines A-B];
5. 引用作为证据时,把对应源码行逐字拷贝到正文代码块中(校验器会用它反查真实文件核验行号,逐字拷贝才能通过);
6. 输出为 markdown 正文(无围栏包裹),以 `# 标题` 开始,长度控制在 150~400 行。

页面结构信息:
{page_spec}

相邻模块接口摘要(可提及,不可深入):
{neighbor_summaries}

源码片段(唯一事实来源):
<source>
{packed_source}
</source>
